
# Written by `next dev` itself, from Next 16.3 on: see
# node_modules/next/dist/server/lib/generate-agent-files.js. They are
# instructions for coding agents, generated by a dependency rather than
# written here, and they re-appear on every dev run. Ignored so a dev server
# does not leave the tree dirty, which is what stops a benchmark certifying
# the commit it measured.
AGENTS.md
CLAUDE.md
