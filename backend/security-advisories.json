{
  "$comment": [
    "Recorded dispositions for production dependency advisories.",
    "The audit gate fails on any advisory that is not listed here, so a new one",
    "has to be looked at rather than accumulating in a report nobody reads.",
    "Every entry needs an owner and an expiresOn date. The gate fails once that",
    "date passes, so an acceptance is a decision with an end rather than a",
    "permanent exemption nobody revisits. Owners are roles, never people."
  ],
  "accepted": [
    {
      "id": "GHSA-ggr8-5vv4-36mx",
      "module": "deepmerge-ts",
      "version": "7.1.5",
      "severity": "high",
      "title": "Stack exhaustion when merging recursive object graphs",
      "path": "@prisma/client -> prisma -> @prisma/config -> deepmerge-ts",
      "disposition": "not-exploitable",
      "reason": [
        "Reached only through Prisma's configuration loader, which runs at CLI",
        "and startup time against prisma.config.ts and prisma.indexer.config.ts.",
        "Those are files in this repository, not request input. No source file",
        "imports deepmerge-ts, and no HTTP handler reaches it.",
        "The advisory needs an attacker-supplied recursive object graph; nothing",
        "on any request path merges one."
      ],
      "recheckWhen": "prisma or @prisma/config is upgraded",
      "recordedOn": "2026-08-27",
      "owner": "explorer-maintainers",
      "expiresOn": "2026-11-27"
    },
    {
      "id": "GHSA-3f6p-5ww8-9rcr",
      "module": "mysql2",
      "version": "3.15.3",
      "severity": "high",
      "title": "Auth plugin downgrade to mysql_clear_password leaks plaintext credentials",
      "path": "@prisma/client -> prisma -> mysql2",
      "disposition": "not-exploitable",
      "reason": [
        "The advisory needs this process to open a MySQL connection: a hostile",
        "server answers the handshake by asking for mysql_clear_password, and",
        "the client sends the password in the clear.",
        "This backend speaks only PostgreSQL. Both clients connect through",
        "@prisma/adapter-pg (src/db.ts and src/indexer/db.ts), no source file",
        "imports mysql2, and no configuration names a MySQL URL. The driver is",
        "present because the Prisma CLI ships every driver it can target."
      ],
      "recheckWhen": "prisma is upgraded, or any MySQL connection is added",
      "recordedOn": "2026-09-02",
      "owner": "explorer-maintainers",
      "expiresOn": "2026-12-02"
    }
  ]
}
