# Backend API base URL. Leave empty for same-origin: the Vite dev proxy in
# development, or a reverse-proxy fronting both apps in production. Set to the
# backend origin for split (cross-origin) deployments, e.g.
# VITE_API_BASE_URL="https://api.example.com"
VITE_API_BASE_URL=""
